#include <iostream>
#include <cstdio>

using namespace std;

int main() {
  freopen("main.in", "r", stdin);
  freopen("main.out", "w", stdout);
  long long a, b;
  cin >> a >> b;
  cout << a << " + " << b << " = " << (a + b) << endl;
  return 0;
}
