Работа с функциями (задание)
============================

Функция, определяющая минимум:
```cpp
int min(int a, int b) {
  return (a < b) ? a : b;
}
```

Вызов:
```cpp 
cout << "min(" << d1 << ", " << d2 << ") = " << min(d1, d2) << endl;
```

Задача: написать функцию, определяющую максимум